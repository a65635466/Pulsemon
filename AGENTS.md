# PulseMon AI Agent Guide

이 문서는 PulseMon 프로젝트를 개발하는 AI 에이전트가 따라야 할 기준 문서입니다. 에이전트는 이 파일을 프로젝트의 최상위 지침으로 삼고, 코드 작성 전에 목표, 범위, 구조, 금지사항, 완료 기준을 확인해야 합니다.

## 1. 프로젝트 목표

PulseMon은 Windows에서 실행되는 가벼운 실시간 시스템 상태 모니터링 프로그램이다.

1차 버전의 목표는 다음과 같다.

- Windows 시스템 트레이에 PulseMon 아이콘을 표시한다.
- 트레이 아이콘 클릭 시 작은 상태창을 표시한다.
- 상태창에서 CPU, RAM, GPU, 네트워크 상태를 실시간으로 확인할 수 있다.
- 상태값은 약 1초 간격으로 자동 갱신된다.
- 프로그램은 백그라운드에서 조용히 실행되며, 사용자가 명시적으로 종료할 수 있다.

예상 사용자 경험:

```text
PulseMon 실행
     ↓
시스템 트레이 아이콘 생성
     ↓
아이콘 클릭
     ↓
┌─────────────────────┐
│ CPU   23%           │
│ RAM   8.2 / 16 GB   │
│ GPU   14% / 52°C    │
│ ↓ 32 Mbps ↑ 4 Mbps │
└─────────────────────┘
```

## 2. 1차 개발 범위

반드시 포함할 기능:

- CPU 사용률 표시
- RAM 사용량 및 전체 용량 표시
- GPU 사용률 표시
- GPU 온도 표시
- 다운로드 속도 표시
- 업로드 속도 표시
- Windows 시스템 트레이 아이콘 표시
- 트레이 아이콘 클릭 시 작은 상태창 표시
- 상태값 자동 갱신
- 프로그램 종료 메뉴 또는 버튼

1차 버전에서 제외할 기능:

- 웹 대시보드
- 서버 연동
- 로그인
- 사용자 계정
- 데이터베이스 저장
- 장기 히스토리 그래프
- 클라우드 동기화
- 모바일 앱
- 자동 업데이트
- 설치 프로그램 제작

## 3. 기술 스택

기본 기술 스택:

- 언어: C#
- 런타임: .NET
- UI 프레임워크: WPF
- 대상 OS: Windows
- 개발 환경: VS Code 또는 Visual Studio
- 버전 관리: Git

권장 프로젝트 생성 명령:

```powershell
dotnet new wpf -n PulseMon
cd PulseMon
dotnet run
```

권장 패키지는 구현 단계에서 필요성이 확인된 뒤 추가한다. 특히 GPU 정보 수집 라이브러리는 하드웨어 호환성, 유지보수 상태, 라이선스를 확인한 뒤 선택한다.

## 4. 권장 디렉터리 구조

```text
PulseMon/
├── App.xaml
├── App.xaml.cs
├── MainWindow.xaml
├── MainWindow.xaml.cs
├── PulseMon.csproj
│
├── Models/
│   └── SystemStatus.cs
│
├── Monitoring/
│   ├── CpuMonitor.cs
│   ├── MemoryMonitor.cs
│   ├── NetworkMonitor.cs
│   └── GpuMonitor.cs
│
├── Services/
│   └── MonitoringService.cs
│
├── Tray/
│   └── TrayManager.cs
│
└── UI/
    └── 필요한 경우 UI 보조 컴포넌트 추가
```

`bin/`과 `obj/`는 빌드 산출물이므로 직접 수정하지 않는다.

## 5. 컴포넌트 책임

### App.xaml / App.xaml.cs

- 애플리케이션 시작과 종료 흐름을 관리한다.
- 전역 리소스와 생명주기를 관리한다.
- 트레이 기반 실행 정책을 초기화한다.

### MainWindow.xaml

- 사용자가 보는 작은 상태창의 레이아웃을 정의한다.
- 모니터링 데이터를 표시하는 UI만 담당한다.
- 하드웨어 정보 수집 로직을 직접 포함하지 않는다.

### MainWindow.xaml.cs

- UI 이벤트와 데이터 바인딩 연결을 담당한다.
- 창 표시, 숨김, 위치 조정 같은 UI 동작을 처리한다.
- 시스템 상태 수집 로직은 서비스나 모니터 클래스에 위임한다.

### Models/SystemStatus.cs

- 현재 시스템 상태를 하나의 데이터 객체로 표현한다.
- CPU, RAM, GPU, 네트워크 값을 포함한다.
- 단위는 명확하게 유지한다.

권장 속성:

```text
CpuUsagePercent
MemoryUsedGb
MemoryTotalGb
GpuUsagePercent
GpuTemperatureCelsius
DownloadMbps
UploadMbps
UpdatedAt
```

### Monitoring/CpuMonitor.cs

- 현재 CPU 사용률을 가져온다.
- 반환값은 0부터 100 사이의 퍼센트 값으로 정규화한다.
- 초기 측정 시 값이 부정확할 수 있음을 고려한다.

### Monitoring/MemoryMonitor.cs

- 사용 중인 RAM과 전체 RAM을 가져온다.
- UI에는 GB 단위로 표시할 수 있게 값을 제공한다.

### Monitoring/NetworkMonitor.cs

- 다운로드 및 업로드 속도를 계산한다.
- 이전 측정값과 현재 측정값의 차이를 기반으로 초당 전송량을 계산한다.
- 네트워크 어댑터가 여러 개인 경우 합산 기준을 명확히 한다.

### Monitoring/GpuMonitor.cs

- GPU 사용률과 온도를 가져온다.
- 하드웨어나 드라이버에 따라 값을 가져오지 못할 수 있으므로 실패 가능성을 정상 시나리오로 처리한다.
- GPU 값 수집 실패가 앱 전체 오류로 이어지면 안 된다.

### Services/MonitoringService.cs

- 각 모니터 클래스를 조합해 `SystemStatus`를 생성한다.
- 약 1초 간격으로 상태를 갱신한다.
- UI와 모니터링 구현 사이의 중간 계층 역할을 한다.

### Tray/TrayManager.cs

- Windows 시스템 트레이 아이콘을 생성하고 관리한다.
- 클릭, 우클릭 메뉴, 종료 동작을 처리한다.
- 앱 종료 시 트레이 아이콘 리소스를 정리한다.

## 6. 개발 단계

### 0단계: 목표 고정

- 1차 버전 범위를 확인한다.
- 제외 기능을 임의로 추가하지 않는다.
- 완료 화면과 사용자 흐름을 먼저 고정한다.

### 1단계: WPF 프로젝트 생성

- `dotnet new wpf -n PulseMon`으로 프로젝트를 생성한다.
- `dotnet run`으로 기본 창 실행을 확인한다.
- 첫 커밋을 만든다.

### 2단계: 기본 구조 생성

- `Models/`, `Monitoring/`, `Services/`, `Tray/`, `UI/` 폴더를 만든다.
- 빈 클래스 파일을 추가한다.
- 빌드가 깨지지 않는지 확인한다.

### 3단계: SystemStatus 모델 구현

- 상태 데이터 모델을 만든다.
- 단위와 기본값을 명확히 한다.
- 표시 불가 상태를 표현할 수 있게 nullable 또는 상태 문자열을 검토한다.

### 4단계: CPU와 RAM 구현

- CPU 사용률 측정을 먼저 구현한다.
- RAM 사용량과 전체 용량 측정을 구현한다.
- GPU와 네트워크보다 CPU/RAM을 먼저 안정화한다.

### 5단계: UI 실시간 표시

- MainWindow에 CPU/RAM 값을 표시한다.
- 1초 간격 갱신을 연결한다.
- UI 스레드 업데이트 규칙을 지킨다.

### 6단계: 트레이 아이콘 구현

- 앱 실행 시 트레이 아이콘을 만든다.
- 트레이 아이콘 클릭 시 상태창을 표시하거나 숨긴다.
- 종료 메뉴를 추가한다.

### 7단계: 네트워크 속도 구현

- 다운로드 및 업로드 속도를 계산한다.
- 단위를 Mbps로 표시한다.
- 어댑터 선택 또는 합산 방식의 기준을 코드에 명확히 반영한다.

### 8단계: GPU 구현

- GPU 사용률과 온도 수집 방식을 결정한다.
- 지원되지 않는 환경에서는 `N/A` 또는 비활성 상태로 표시한다.
- GPU 오류가 CPU/RAM/네트워크 표시를 방해하지 않게 한다.

### 9단계: 안정화

- 빌드, 실행, 종료, 트레이 동작을 반복 확인한다.
- 예외 처리와 리소스 정리를 점검한다.
- 불필요한 로그와 테스트 코드를 정리한다.

## 7. 코딩 규칙

- C# 표준 네이밍 규칙을 따른다.
- 클래스와 파일명은 일치시킨다.
- 하나의 클래스는 하나의 주요 책임만 가진다.
- UI 코드에서 하드웨어 상태를 직접 수집하지 않는다.
- 모니터링 로직은 `Monitoring/` 또는 `Services/`에 둔다.
- 단위가 있는 값은 속성명에 단위를 포함한다.
- 매직 넘버는 상수로 분리한다.
- 비동기 작업은 UI 스레드를 막지 않게 작성한다.
- 예외를 조용히 삼키지 않는다. 다만 사용자에게 불필요한 오류 팝업을 반복 표시하지 않는다.
- 주석은 복잡한 의사결정이나 하드웨어별 예외 상황을 설명할 때만 사용한다.

## 8. 안전한 변경 원칙

- 한 번에 하나의 기능 단위만 변경한다.
- 변경 전 현재 빌드 상태를 확인한다.
- 기존 동작을 삭제하기 전에 사용처를 확인한다.
- 불필요한 리팩터링을 기능 구현과 섞지 않는다.
- 사용자가 작성한 코드나 설정을 임의로 되돌리지 않는다.
- `bin/`, `obj/`, `.vs/` 같은 생성물은 수정 대상으로 보지 않는다.
- 외부 패키지 추가 전 필요성, 라이선스, 유지보수 상태를 확인한다.
- GPU 관련 구현은 실패 가능성을 전제로 작성한다.

## 9. 테스트 및 빌드 규칙

기본 확인 명령:

```powershell
dotnet build
dotnet run
```

변경 후 확인할 항목:

- 프로젝트가 빌드되는가
- 앱이 실행되는가
- 창이 정상 표시되는가
- 트레이 아이콘이 생성되는가
- 트레이 아이콘 클릭 동작이 정상인가
- 종료 시 프로세스와 트레이 아이콘이 정리되는가
- CPU/RAM 값이 비정상 범위로 표시되지 않는가
- 네트워크 속도가 음수로 표시되지 않는가
- GPU 정보를 가져오지 못해도 앱이 계속 실행되는가

테스트 프로젝트를 추가할 경우 권장 구조:

```text
PulseMon.Tests/
├── MonitoringServiceTests.cs
├── NetworkMonitorTests.cs
└── SystemStatusTests.cs
```

UI 자동 테스트는 1차 버전에서 필수는 아니지만, 계산 로직은 가능한 한 단위 테스트 가능한 구조로 분리한다.

## 10. 예외 처리 원칙

- 모니터링 실패는 앱 전체 실패로 처리하지 않는다.
- 특정 센서 값을 가져오지 못하면 해당 값만 `N/A` 또는 null 상태로 둔다.
- 반복 발생 가능한 예외는 로그 또는 내부 상태로 관리하고, 사용자에게 팝업을 반복 표시하지 않는다.
- 타이머, 트레이 아이콘, 시스템 리소스는 앱 종료 시 정리한다.
- UI 스레드가 아닌 곳에서 UI 요소를 직접 수정하지 않는다.
- 네트워크 어댑터가 없거나 비활성화된 경우 속도는 0 또는 표시 불가 상태로 처리한다.

## 11. 성능 기준

- 기본 갱신 주기는 약 1초로 한다.
- 상태 수집 때문에 UI가 멈추면 안 된다.
- 백그라운드 실행 중 CPU 사용률이 과도하게 올라가면 안 된다.
- 불필요하게 많은 객체를 매초 생성하지 않는다.
- 장시간 실행해도 메모리 사용량이 계속 증가하지 않아야 한다.
- 트레이 앱 특성상 화면을 차지하지 않고 조용히 실행되어야 한다.

권장 기준:

```text
상태 갱신 주기: 약 1초
일반 대기 CPU 사용률: 낮게 유지
UI 반응성: 즉시 반응
종료 동작: 잔여 프로세스 없이 종료
```

## 12. 금지사항

- 1차 버전에 서버, 로그인, DB, 웹 대시보드를 추가하지 않는다.
- UI 파일에 CPU/RAM/GPU/네트워크 수집 코드를 직접 넣지 않는다.
- GPU 값을 반드시 가져올 수 있다고 가정하지 않는다.
- 실패한 센서 하나 때문에 앱 전체를 종료하지 않는다.
- 매초 무거운 외부 프로세스를 실행해 값을 수집하지 않는다.
- 사용자의 기존 변경사항을 임의로 되돌리지 않는다.
- 빌드 산출물이나 임시 파일을 커밋하지 않는다.
- 예외를 빈 `catch` 블록으로 무시하지 않는다.
- 불필요한 대형 프레임워크나 패키지를 추가하지 않는다.
- 검증 없이 인터넷 예제 코드를 그대로 붙여 넣지 않는다.

## 13. 에이전트 작업 방식

AI 에이전트는 다음 순서로 작업한다.

1. 현재 요청을 읽고 변경 범위를 한 문장으로 정의한다.
2. 관련 파일만 먼저 확인한다.
3. 기존 구조와 네이밍을 따른다.
4. 필요한 최소 변경을 구현한다.
5. 빌드 또는 실행으로 검증한다.
6. 변경한 파일과 검증 결과를 사용자에게 보고한다.

에이전트 응답 원칙:

- 추측한 내용은 추측이라고 밝힌다.
- 구현하지 않은 기능을 완료했다고 말하지 않는다.
- 빌드나 테스트를 실행하지 못했다면 이유를 말한다.
- 큰 작업은 작은 단계로 나누어 진행한다.
- 사용자가 요구하지 않은 기능을 임의로 확장하지 않는다.
- 코드 변경 전후의 의도를 간단히 설명한다.

작업 단위 예시:

```text
좋은 작업 단위:
- SystemStatus 모델 추가
- CpuMonitor 구현
- MainWindow에 CPU/RAM 표시 연결
- TrayManager 추가

나쁜 작업 단위:
- 전체 앱 한 번에 완성
- UI 전면 개편과 GPU 구현을 동시에 진행
- 패키지 여러 개를 이유 없이 추가
```

## 14. 완료 정의

기능 하나가 완료되었다고 말하려면 다음 조건을 만족해야 한다.

- 코드가 빌드된다.
- 기존 기능을 깨뜨리지 않는다.
- 요청한 동작이 실제로 구현되어 있다.
- 실패 가능한 상황에 대한 처리가 있다.
- 관련 파일의 책임 경계를 지켰다.
- 사용자가 다음에 무엇을 확인하면 되는지 알 수 있다.

1차 버전 전체 완료 기준:

- 앱 실행 시 트레이 아이콘이 생성된다.
- 트레이 아이콘 클릭 시 상태창이 열린다.
- CPU 사용률이 표시된다.
- RAM 사용량과 전체 용량이 표시된다.
- 네트워크 다운로드 및 업로드 속도가 표시된다.
- GPU 사용률과 온도가 표시되거나, 지원되지 않는 경우 명확히 표시 불가 상태가 나온다.
- 상태값이 약 1초 간격으로 갱신된다.
- 종료 메뉴 또는 버튼으로 앱을 종료할 수 있다.
- 종료 후 트레이 아이콘과 프로세스가 남지 않는다.
- `dotnet build`가 성공한다.

## 15. 예시 프롬프트

### 프로젝트 초기화

```text
PulseMon WPF 프로젝트의 기본 디렉터리 구조를 만들어줘.
AGENTS.md의 규칙을 따르고, 먼저 빌드가 깨지지 않는 최소 변경만 해줘.
```

### 모델 구현

```text
SystemStatus 모델을 구현해줘.
CPU, RAM, GPU, 네트워크 상태를 담되 단위가 속성명에 드러나게 해줘.
변경 후 dotnet build로 확인해줘.
```

### CPU/RAM 구현

```text
CpuMonitor와 MemoryMonitor를 구현해줘.
UI에는 아직 연결하지 말고, MonitoringService에서 SystemStatus로 조합할 수 있게 만들어줘.
```

### UI 연결

```text
MainWindow에 CPU 사용률과 RAM 사용량을 1초마다 표시하게 연결해줘.
UI 코드가 직접 하드웨어 정보를 읽지 않도록 AGENTS.md 구조를 지켜줘.
```

### 트레이 구현

```text
Windows 시스템 트레이 아이콘을 추가해줘.
아이콘 클릭 시 MainWindow가 보이거나 숨겨지고, 우클릭 메뉴에서 종료할 수 있게 해줘.
```

### 네트워크 구현

```text
NetworkMonitor를 구현해줘.
다운로드와 업로드 속도를 Mbps로 계산하고, 음수 값이 표시되지 않게 처리해줘.
```

### GPU 구현

```text
GpuMonitor를 구현해줘.
GPU 사용률과 온도를 가져오되, 지원되지 않는 환경에서는 앱이 죽지 않고 표시 불가 상태로 처리해줘.
필요한 패키지가 있다면 추가 이유와 대안을 먼저 설명해줘.
```

### 안정화

```text
PulseMon의 종료 흐름과 리소스 정리를 점검해줘.
트레이 아이콘이 종료 후 남지 않는지 확인하고, 필요한 수정만 해줘.
```

### 코드 리뷰

```text
AGENTS.md 기준으로 현재 PulseMon 코드를 리뷰해줘.
버그, 책임 분리 위반, 예외 처리 누락, 빌드/테스트 누락을 우선순위별로 알려줘.
```

## 16. 에이전트 최종 보고 형식

작업 완료 후 에이전트는 다음 내용을 짧게 보고한다.

```text
변경한 내용:
- ...

확인한 내용:
- dotnet build 성공
- 필요한 경우 dotnet run 확인

주의할 점:
- ...

다음 추천 작업:
- ...
```

빌드나 실행 확인을 하지 못한 경우:

```text
검증 제한:
- dotnet build를 실행하지 못했습니다. 이유: ...
```

## 17. 우선순위

갈등이 생기면 다음 우선순위를 따른다.

1. 사용자 요청
2. 앱이 빌드되고 실행되는 상태 유지
3. 1차 버전 범위 유지
4. 책임 분리와 안전한 변경
5. UI 개선
6. 추가 기능

PulseMon은 먼저 작고 안정적으로 완성되어야 한다. 기능 확장은 1차 버전이 안정적으로 동작한 뒤 진행한다.
