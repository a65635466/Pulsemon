<Window x:Class="PulseMon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PulseMon"
        Title="PulseMon"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#15181D" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#DF1F252D" />
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#18FFFFFF" />
        <SolidColorBrush x:Key="BorderBrush" Color="#24FFFFFF" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#F5F7FA" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#9AA4AF" />
        <SolidColorBrush x:Key="LabelTextBrush" Color="#C9D1D9" />
        <SolidColorBrush x:Key="AccentBrush" Color="#36D6E7" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#45E08F" />
        <SolidColorBrush x:Key="MutedTrackBrush" Color="#24FFFFFF" />

        <Style x:Key="MetricLabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource LabelTextBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Medium" />
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="7" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Background" Value="{DynamicResource MutedTrackBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid Height="{TemplateBinding Height}">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4" />
                            <Border HorizontalAlignment="Left"
                                    Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ActualWidth}"
                                    CornerRadius="4">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Converter={x:Static local:MainWindow.ProgressScaleConverter}}" />
                                </Border.RenderTransform>
                                <Border.RenderTransformOrigin>0,0.5</Border.RenderTransformOrigin>
                                <Border Background="{TemplateBinding Foreground}"
                                        CornerRadius="4" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Border x:Name="RootPanel"
            Width="320"
            Padding="16"
            Background="{DynamicResource PanelBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="24"
            MouseLeftButtonDown="OnRootPanelMouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="14" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="14" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <Border Width="34"
                            Height="34"
                            Margin="0,0,10,0"
                            Background="{DynamicResource CardBackgroundBrush}"
                            CornerRadius="17">
                        <Viewbox Margin="6">
                            <Canvas Width="32" Height="32">
                                <Ellipse Width="22" Height="22" Canvas.Left="5" Canvas.Top="5"
                                         Stroke="#2D343D" StrokeThickness="4" />
                                <Path Data="M16,5 A11,11 0 0 1 27,16"
                                      Stroke="#36D6E7" StrokeThickness="4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                <Path Data="M5,16 L11,16 L13,12 L17,21 L20,16 L27,16"
                                      Stroke="{DynamicResource PrimaryTextBrush}" StrokeThickness="2"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
                            </Canvas>
                        </Viewbox>
                    </Border>

                    <StackPanel>
                        <TextBlock Text="PulseMon"
                                   FontSize="17"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}" />
                        <TextBlock Text="Live now"
                                   FontSize="12"
                                   Foreground="{DynamicResource SecondaryTextBrush}" />
                    </StackPanel>
                </StackPanel>

                <TextBlock x:Name="UpdatedAtText"
                           Grid.Column="1"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="12"
                           Text="--:--:--" />
                <Button Grid.Column="3"
                        Width="22"
                        Height="22"
                        Padding="0"
                        VerticalAlignment="Center"
                        Click="OnHideButtonClicked"
                        Content="X" />
            </Grid>

            <Border Grid.Row="2"
                    Padding="12"
                    Background="{DynamicResource CardBackgroundBrush}"
                    CornerRadius="18">
                <StackPanel>
                    <TextBlock x:Name="DeviceModelText"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Text="Device" />
                    <TextBlock x:Name="DeviceCpuText"
                               Margin="0,5,0,0"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               FontSize="12"
                               Text="CPU" />
                    <TextBlock x:Name="DeviceGpuText"
                               Margin="0,3,0,0"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               FontSize="12"
                               Text="GPU" />
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="4">
                <Grid Margin="0,0,0,13">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="74" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="CPU" Style="{StaticResource MetricLabelStyle}" />
                    <ProgressBar x:Name="CpuProgressBar" Grid.Column="1" />
                    <TextBlock x:Name="CpuUsageText" Grid.Column="2" Style="{StaticResource MetricValueStyle}" Text="0%" />
                </Grid>

                <Grid Margin="0,0,0,13">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="74" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="RAM" Style="{StaticResource MetricLabelStyle}" />
                    <ProgressBar x:Name="MemoryProgressBar" Grid.Column="1" Foreground="{DynamicResource SuccessBrush}" />
                    <TextBlock x:Name="MemoryUsageText" Grid.Column="2" Style="{StaticResource MetricValueStyle}" Text="- / - GB" />
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="74" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="GPU" Style="{StaticResource MetricLabelStyle}" />
                    <ProgressBar x:Name="GpuProgressBar" Grid.Column="1" />
                    <TextBlock x:Name="GpuStatusText" Grid.Column="2" Style="{StaticResource MetricValueStyle}" Text="N/A" />
                </Grid>
            </StackPanel>

            <Border Grid.Row="6"
                    Padding="12"
                    Background="{DynamicResource CardBackgroundBrush}"
                    CornerRadius="18">
                <TextBlock x:Name="NetworkSpeedText"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           FontSize="13"
                           FontWeight="Medium"
                           Text="D - Mbps  U - Mbps" />
            </Border>
        </Grid>
    </Border>
</Window>
